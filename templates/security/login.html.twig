{% extends "base.html.twig" %}

{% block title %}
	Login
{% endblock %}

{% block body %}
	<h1>Login</h1>

	{{ form_start(formView) }}
	{{ form_widget(formView) }}
	{{ form_errors(formView) }}

	<button type="submit">
		<i class="fas fa-save"></i>
        Enregistrer
	</button>

	{{ form_end(formView) }}
{% endblock %}
