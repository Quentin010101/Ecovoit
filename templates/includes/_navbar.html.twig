<header class="row">
	<div class="icon-container">
		<i class="fas fa-car"></i>
	</div>
	<nav>
		<ul class="row">
			<li class="nav-item">
				<a href="{{ path('homepage')}}" class="nav-link">Accueil</a>
			</li>
			<li class="nav-item">
				<a href="{{ path('findRide_search') }}" class="nav-link">Trajet</a>
			</li>
		</ul>
	</nav>
	<div class="log">
		<ul class="row">
		{% if app.user %}
			<p>Bonjour: {{ app.user.name }}</p>
			<li class="nav-item">
				<a href="{{ path('security_logout') }}" class="nav-link">Log Out</a>
			</li>
		{% else %}
			<li class="nav-item">
				<a href="{{ path('security_login') }}" class="nav-link">Log In</a>
			</li>
			<li class="nav-item">
				<a href="{{ path('security_inscription') }}" class="nav-link">Inscription</a>
			</li>		
		{% endif %}
		</ul>
	</div>
</header>
